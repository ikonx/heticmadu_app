import React, { FunctionComponent } from 'react';
import TagsList from '@components/molecules/TagsList/TagsList';

interface Props {}

const TagsListStories: FunctionComponent<Props> = () => {
  return (
    <TagsList />
  );
};

export default TagsListStories;
