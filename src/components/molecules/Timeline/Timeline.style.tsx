import { View } from 'react-native';
import styled from 'styled-components';

export const StyledTimeline = styled(View)`
    flex-direction: row;
    align-items: center;
    margin: 0 auto;
    width: 260px;
    height: 80px;
`;
