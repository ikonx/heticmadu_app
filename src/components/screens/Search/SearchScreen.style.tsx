import { View } from 'react-native';
import styled from 'styled-components';

export const StyledSearchScreen = styled(View)`
  background-color: ${({ theme }) => theme.Colors.mainWhite};
  flex: 1;
`;
