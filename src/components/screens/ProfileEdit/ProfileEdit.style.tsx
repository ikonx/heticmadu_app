import styled from 'styled-components';
import { SafeAreaView } from 'react-native-safe-area-context';

export const ProfileEditContainer = styled(SafeAreaView)`
  padding: 0 16px;
  margin-top: 32px;
  align-items: center;
`;
